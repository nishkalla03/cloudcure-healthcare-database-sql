# cloudcure-healthcare-database-sql
SQL implementation for a healthcare database management system (CloudCure), including SQL queries, joins, views, filtering, and database operations using Oracle APEX.
